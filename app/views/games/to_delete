<% character.gains.each do |gain| %>
<p>1 - <%= gain.id %></p>
<span><%= link_to gain_path(gain.id) , method: :delete, data: { confirm: "Do you want to delete this Skill from this character?" } do %>
  <% end %>
    <i class="fas fa-trash-alt"></i></span>
  <% end %>
